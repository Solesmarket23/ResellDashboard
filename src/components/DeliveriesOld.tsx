import React from 'react';

/**
 * Deprecated component (kept only to preserve old import paths / history).
 * The active deliveries UI lives elsewhere.
 */
export default function DeliveriesOld() {
  return (
    <div className="p-6 text-sm text-gray-600">
      Deliveries (legacy view) has been deprecated.
    </div>
  );
}

